#include <stdio.h>
#include <stdlib.h>

#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED

#endif // FUNCIONES_H_INCLUDED

float sumar (float x, float y);
float restar (float x, float y);
float dividir (float x, float y);
float multiplicar (float x, float y);
float factorial (float x);

/** sumar
 * \brief suma los valores a y b
 * \param primer valor (a)
 * \param segundo valor (B)
 * \return devuelve el resultado de la suma de ambos valores
 */

/** restar
 * \brief resta los valores a y b
 * \param primer valor (a)
 * \param segundo valor (b)
 * \return devuelve el resultado de la resta de ambos valores
 */

/** dividir
 * \brief divide los valores a y b
 * \param primer valor (a)
 * \param segundo valor (b)
 * \return devuelve el resultado de la division de ambos valores
 */

/** multiplicar
 * \brief multiplica los valores a y b
 * \param primer valor (a)
 * \param segundo valor (b)
 * \return devuelve el resultado de la multiplicacion de ambos valores
 */

 /** factorial
  * \brief saca el factorial del valor A=X
  * \param numero a factorear
  * \return devuelve el numero factorizado (resultado)
  */



